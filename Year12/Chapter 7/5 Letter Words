words = ['hello', 'goodbye', 'hola', 'konnichiwa']
count = 0

for j in words:
    for i in range(len(words)):
        if len(words[i]) > 5:
            count = count + 1
            print(count)
        else: 
            print("Not 5 letters or more")
